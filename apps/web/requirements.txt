# Vercel Functions use BaseHTTPRequestHandler (no Flask needed in production)
# Flask is only needed for local development
flask==3.0.0
flask-cors==4.0.0
requests==2.31.0
python-dotenv==1.0.1

# Sign in with Apple token revocation on account deletion (_apple.py)
pyjwt==2.8.0
cryptography==42.0.5
